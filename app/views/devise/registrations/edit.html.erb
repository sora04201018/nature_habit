<div class="grow flex items-center justify-center p-4 sm:p-8">
  <div class="w-full max-w-lg p-8 bg-white/70 shadow-lg rounded-3xl">
    <h1 class="text-2xl sm:text-3xl md:text-4xl font-bold text-green-700 mb-6 sm:mb-8 text-center">🌱ユーザー情報編集🌱</h1>
    <%= form_with model: resource, url: registration_path(resource_name), method: :put, local: true do |f| %> <%# resource_nameの引数渡す %>
      <%= render "shared/devise_errors_message", resource: resource %>

      <div class="mb-5">
        <%= f.label :name, "ユーザー名", class: "block text-gray-700 font-semibold mb-1" %>
        <%= f.text_field :name, class: "w-full border border-gray-300 p-2 rounded-lg focus:ring-2 focus:ring-green-400 focus:outline-none" %>
      </div>

      <div class="flex flex-col mb-5">
        <%= f.submit "更新", class: "bg-green-500 w-full text-center hover:bg-green-600 text-white font-semibold py-3 px-6 rounded-lg shadow-md transition" %>
        <%= link_to "マイページへ戻る", mypage_path, class: "mt-2 text-center w-full bg-gray-300 hover:bg-gray-600 text-white font-semibold py-3 px-5 rounded-lg shadow-md transition" %>
      </div>
    <% end %>
  </div>
</div>
