<div class="min-h-screen flex flex-col w-screen bg-gradient-to-b from-green-50 to-white">
  <div class="flex-grow flex items-center justify-center p-8">
    <div class="w-full max-w-lg p-8">
      <h1 class="text-4xl font-bold text-green-700 mb-10 text-center">🌱ログイン🌱</h1>
        <%= form_with model: resource, url: session_path(resource_name), local: true do |f| %> <%# form_with turbo対応の書き方 %>
          <div class="mb-8">
            <%= f.label :email, "メールアドレス", class: "block text-gray-700 font-semibold mb-1" %>
            <%= f.text_field :email, class: "w-full border border-gray-300 p-2 rounded-lg focus:ring-2 focus:ring-green-400 focus:outline-none", placeholder: "xxx@example.com" %>
          </div>
          <div class="mb-8">
            <%= f.label :password, "パスワード", class: "block text-gray-700 font-semibold mb-1" %>
            <%= f.password_field :password, class: "w-full border border-gray-300 p-2 rounded-lg focus:ring-2 focus:ring-green-400 focus:outline-none" %>
          </div>
          <div class="flex justify-end mb-8">
            <%= f.submit "ログイン", class: "bg-green-500 hover:bg-green-600 text-white font-semibold py-3 px-5 rounded-lg shadow-md transition" %>
          </div>
        <% end %>
        <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
