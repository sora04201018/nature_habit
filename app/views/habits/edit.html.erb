<div class="flex flex-grow items-center justify-center p-8">
  <div class="w-full max-w-lg p-8">
    <h1 class="text-center font-bold text-4xl text-green-700 mb-10">🌱習慣の編集🌱</h1>
    <%= render "shared/habit_errors_message", model: @habit %>
    <%= render "form", habit: @habit %>
  </div>
</div>
