<div class="flex flex-grow items-start justify-center p-8">
  <div class="w-full max-w-6xl">
    <h1 class="text-center text-4xl font-bold text-green-700 mb-10">🌱習慣一覧🌱</h1>

    <% if @habits.present? %>
      <!-- ▼ ここで 3列のグリッドレイアウトを作る -->
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
        <% @habits.each do |habit| %>
          <!-- ▼ カードデザイン -->
          <div class="bg-white shadow-lg border border-green-300 border-3 rounded-xl p-6 hover:shadow-xl transition">
            <h2 class="text-green-700 font-bold text-xl mb-4">🌿 <%= habit.title %></h2>

            <p class="text-gray-700 mb-2">
              <span class="font-semibold">頻度：</span>
              <%= habit.frequency.humanize %>
            </p>

            <p class="text-gray-700">
              <span class="font-semibold">作成日：</span>
              <%= l habit.created_at.in_time_zone('Asia/Tokyo'), format: :short %>
            </p>
          </div>
        <% end %>
      </div>

    <% else %>
      <p class="text-center text-gray-600">登録されている習慣はありません。</p>
    <% end %>
  </div>
</div>
<%= render "back" %>
