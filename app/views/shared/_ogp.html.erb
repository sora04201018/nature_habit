<%
  hashtags = %w[
    NatureHabit
    自然習慣
    自然と暮らす
    エコライフ
    習慣化
    自然界隈
  ]

  base_text =
    if type == :habit
      "習慣名「#{title}」🌿"
    elsif type == :post
      "投稿名「#{title}」🌿"
    end

  share_text = <<~TEXT
    自然と向き合いました 🌿
    #{base_text}
    #{hashtags.map { |tag| "##{tag}" }.join(" ")}
  TEXT

  share_url = request.original_url
%>

<div class="flex justify-center mt-6">
  <%= link_to "https://twitter.com/intent/tweet?text=#{ERB::Util.url_encode(share_text)}&url=#{ERB::Util.url_encode(share_url)}",
      target: "_blank",
      rel: "noopener",
      class: "
        inline-flex items-center gap-3
        px-5 py-3
        rounded-full
        bg-green-50 text-green-700
        hover:bg-green-100
        transition
        shadow-sm
        text-sm sm:text-base
      " do %>

    <!-- Xアイコン -->
    <svg viewBox="0 0 24 24" class="w-5 h-5 sm:w-6 sm:h-6 fill-current">
      <path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24h-6.654l-5.214-6.817-5.964 6.817H1.687l7.73-8.84L1.25 2.25h6.82l4.713 6.231L18.244 2.25z"/>
    </svg>

    <span class="font-medium">
      Xでシェアする
    </span>
  <% end %>
</div>
